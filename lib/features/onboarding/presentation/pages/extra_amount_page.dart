import 'package:flutter/material.dart';

/// Extra monthly amount prompt page.
class ExtraAmountPage extends StatelessWidget {
  const ExtraAmountPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement extra amount input
    return const Scaffold(
      body: Center(child: Text('Extra Amount')),
    );
  }
}
