import 'package:flutter/material.dart';

/// Guided first debt entry during onboarding.
class AddFirstDebtPage extends StatelessWidget {
  const AddFirstDebtPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement guided debt entry form
    return const Scaffold(
      body: Center(child: Text('Add First Debt')),
    );
  }
}
