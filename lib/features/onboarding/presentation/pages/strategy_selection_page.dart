import 'package:flutter/material.dart';

/// Strategy selection with real numbers from user's data.
///
/// Feature 1.0 step 4: "tính trên data user vừa nhập"
class StrategySelectionPage extends StatelessWidget {
  const StrategySelectionPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement strategy selection with comparison
    return const Scaffold(
      body: Center(child: Text('Strategy Selection')),
    );
  }
}
