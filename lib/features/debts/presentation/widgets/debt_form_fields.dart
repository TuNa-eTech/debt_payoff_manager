import 'package:flutter/material.dart';

/// Reusable debt form fields widget.
class DebtFormFields extends StatelessWidget {
  const DebtFormFields({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Implement form fields for debt entry/edit
    return const Column(
      children: [Text('Debt Form Fields')],
    );
  }
}
